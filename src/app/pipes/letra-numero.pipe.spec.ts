import { LetraNumeroPipe } from './letra-numero.pipe';

describe('LetraNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new LetraNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
